server.port=${PORT:8080}
spring.application.name=Expense-Tracker

# Kommagetrennte Liste der erlaubten CORS-Quellen (für Render-Frontend etc.)
# Kann über die Umgebungsvariable APP_CORS_ALLOWED_ORIGINS überschrieben werden.
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:5174}

# Datenbank-Konfiguration (Render-PostgreSQL)
spring.datasource.url=jdbc:postgresql://dpg-d616o0sr85hc739pmhn0-a.frankfurt-postgres.render.com/et_db3?sslmode=require
spring.datasource.username=et_db3_user
spring.datasource.password=0M4HFds91Y4LWIXE8A6vh9TH3BfPJv2l

# JPA / Hibernate Einstellungen
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
